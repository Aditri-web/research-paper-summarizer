# =============================================================================
# Runtime requirements — Research Paper Summarizer
# Install inside Colab/Kaggle with:
#   pip install -r requirements.txt
# For local development (CPU-only inference/eval):
#   pip install -r requirements.txt --extra-index-url https://download.pytorch.org/whl/cpu
# =============================================================================

# --- Core framework ---
torch>=2.1.0
transformers>=4.40.0
datasets>=2.19.0
accelerate>=0.29.0
tokenizers>=0.19.0

# --- QLoRA / PEFT ---
peft>=0.10.0
bitsandbytes>=0.43.0

# --- Unsloth (CUDA training) ---
# Uncomment when running on GPU (Colab/Kaggle).
# Unsloth requires CUDA; install via their official one-liner in the notebook.
# unsloth @ git+https://github.com/unslothai/unsloth.git

# --- Supervised Fine-Tuning ---
trl>=0.8.6

# --- Evaluation metrics ---
rouge-score>=0.1.2
bert-score>=0.3.13
# AlignScore — install separately:
#   pip install git+https://github.com/yuh-zha/AlignScore.git
#   python -m spacy download en_core_web_sm

# --- Data / utilities ---
numpy>=1.26.0
pandas>=2.0.0
PyYAML>=6.0
tqdm>=4.66.0
scipy>=1.11.0

# --- Inference / demo ---
gradio>=4.20.0
huggingface-hub>=0.22.0

# --- Visualization (evaluation notebook) ---
matplotlib>=3.8.0
seaborn>=0.13.0

